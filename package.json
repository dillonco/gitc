{
  "name": "gitc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "dev:ui": "vite --host 127.0.0.1 --port 1420",
    "build": "vite build && tauri build",
    "build:ui": "vite build",
    "check": "tsc --noEmit && svelte-check --tsconfig ./tsconfig.json && cargo test --manifest-path src-tauri/Cargo.toml",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "svelte": "^5.0.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@tauri-apps/cli": "^2.0.0",
    "svelte-check": "^4.7.2",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
